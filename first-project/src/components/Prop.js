import React from 'react';

// This is the functional and without jsx elements

const Propsfunction = (property) =>{
    console.log(property.name);
return <h1>hello {property.name}</h1>
}
export default Propsfunction;